const name=  "Vaibhav";

const number =38;

console.log(`My name is ${name} and my repo count is ${number}`);

const newString = "     vaibahv and hitesg   ";

console.log(newString.trim());
console.log(newString.replace("and","or ").trim());
// split is used to make array on the basic of the what we want

